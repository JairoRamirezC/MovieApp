describe("Pruebas en archivo Demo", () => {
  test("should first", () => {
    const msg1 = "hola mundo";

    const msg2 = msg1.trim();

    expect(msg1).toBe(msg2);
  });

  test("should second", () => {
    const msg1 = "hola mundo";

    const msg2 = msg1.trim();

    expect(msg1).toBe(msg2);
  });
});
