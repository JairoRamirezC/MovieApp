# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - primerpagina

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - integration
  # workflow_dispatch: # This allows you to manually trigger the workflow from the GitHub Actions tab in your repository. You can also specify input parameters for the workflow, which can be used to customize the deployment process (e.g., selecting the environment to deploy to).
  #   inputs:
  #     environment:
  #       description: 'Select the deployment environment'
  #       required: true
  #       default: 'Production'
  #       options:
  #         - Production
  #         - Staging

  # repository_dispatch:
  #   types: [deploy] # This allows you to trigger the workflow from an external event, such as a deployment request from another system. You can specify the event type (e.g., 'deploy') and include additional data in the payload if needed.

  # schedule:
  # - cron: '0 0 * * *' # Runs every day at midnight

jobs:
  build:
    strategy:
      matrix:
        platform: [ubuntu-latest, windows-latest, macos-latest]
    
    runs-on: ${{ matrix.platform }}

    # runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
          cache: 'npm'

      - name: npm install, build, and test
        run: |
          echo ${{ secrets.NEW_MESSAGE }} 
          npm install
          npm run test --if-present
          npm run build --if-present

      # - name: Zip artifact for deployment
      #   run: zip release.zip ./* -r

      # - name: Upload artifact for deployment job
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: node-app
      #     path: release.zip

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   environment:
  #     name: 'Production'
  #     url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
  #   permissions:
  #     id-token: write #This is required for requesting the JWT

  #   steps:
  #     - name: Download artifact from build job
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: node-app

  #     - name: Unzip artifact for deployment
  #       run: unzip release.zip
      
  #     - name: Login to Azure
  #       uses: azure/login@v2
  #       with:
  #         client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_17DA29A6813B40A4A2D250344E19B062 }}
  #         tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_1ABB8B67C7394CDAA25F9877BD6EFAB1 }}
  #         subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_4FEB368383D140E4AEC467D660970F6F }}

  #     - name: 'Deploy to Azure Web App'
  #       id: deploy-to-webapp
  #       uses: azure/webapps-deploy@v3
  #       with:
  #         app-name: 'primerpagina'
  #         slot-name: 'Production'
  #         package: .
          